package cmd

const (
	CategoryFlag    = "category"    // -c or --category
	DescriptionFlag = "description" // -D or --description
	AmountFlag      = "amount"      // -a or --amount
	IdFlag          = "id"          // -i or --id
	MonthFlag       = "month"       // -m or --month
	ForceFlag       = "force"       // --force
	OutputFlag      = "output"      // -o or --output
	DelimiterFlag   = "delimiter"   // -d or --delimiter
	DataFlag        = "data"        // --data
)
